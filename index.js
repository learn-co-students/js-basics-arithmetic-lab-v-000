/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!

newID = oldID + 1000000000;

ageIsValid = Number.isInteger();

// a randomly-generated number between 0 (inclusive) and 20 (exclusive)
randomNumber = Math.floor(Math.random() * 10);

// take the value in randomNumber and round it down to the nearest integer
randomInteger = Math.floor(randomNumber)

// convert randomInteger into a valid ID number — an integer between 1000000001 and 1000000020
randomUserID = Number.parseInt('1000000001', 10);
